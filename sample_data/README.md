# Real sample dataset

`wisconsin_breast_cancer.csv` is exported from scikit-learn's real Wisconsin Diagnostic Breast Cancer dataset.

The target column is renamed to `label` so it can be uploaded directly to the platform examples.
